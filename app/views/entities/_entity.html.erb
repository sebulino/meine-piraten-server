<div id="<%= dom_id entity %>">
  <h2><%= entity.name %></h2>

  <div class="detail-grid">
    <div class="detail-item">
      <div class="detail-label">Landesverband (LV)</div>
      <div class="detail-value"><%= entity.LV ? "Ja" : "Nein" %></div>
    </div>

    <div class="detail-item">
      <div class="detail-label">Ortsverband (OV)</div>
      <div class="detail-value"><%= entity.OV ? "Ja" : "Nein" %></div>
    </div>

    <div class="detail-item">
      <div class="detail-label">Kreisverband (KV)</div>
      <div class="detail-value"><%= entity.KV ? "Ja" : "Nein" %></div>
    </div>

    <% if entity.entity_id.present? %>
      <div class="detail-item">
        <div class="detail-label">Übergeordnete Gliederung</div>
        <div class="detail-value"><%= Entity.find_by(id: entity.entity_id)&.name || entity.entity_id %></div>
      </div>
    <% end %>
  </div>
</div>
