<% content_for :title, "Aufgabe bearbeiten" %>

<h1>Aufgabe bearbeiten</h1>

<div class="card">
  <%= render "form", task: @task %>
</div>

<div class="btn-group" style="margin-top: var(--space-md);">
  <%= link_to "Aufgabe anzeigen", @task, class: "btn btn-secondary" %>
  <%= link_to "Zurück zur Liste", tasks_path, class: "btn btn-secondary" %>
</div>
