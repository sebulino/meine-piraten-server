<% content_for :title, "Kategorie bearbeiten" %>

<h1>Kategorie bearbeiten</h1>

<div class="card">
  <%= render "form", category: @category %>
</div>

<div class="btn-group" style="margin-top: var(--space-md);">
  <%= link_to "Kategorie anzeigen", @category, class: "btn btn-secondary" %>
  <%= link_to "Zurück zur Liste", categories_path, class: "btn btn-secondary" %>
</div>
