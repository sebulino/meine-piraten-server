<% content_for :title, "Neue Kategorie" %>

<h1>Neue Kategorie</h1>

<div class="card">
  <%= render "form", category: @category %>
</div>

<div style="margin-top: var(--space-md);">
  <%= link_to "Zurück zur Liste", categories_path, class: "btn btn-secondary" %>
</div>
